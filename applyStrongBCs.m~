function [ M, K, f] = applyStrongBCs( leftDirichletBC, rightDirichletBC )
%APPLYBCS apply Dirichlet BCs
%   left Dirichlet BC
%   right Dirichlet BC

K(1, :) = [];
K(:, size(problem.LM, 1)) = [];

(1, :) = [];
K(:, size(problem.LM, 1)) = [];

end

