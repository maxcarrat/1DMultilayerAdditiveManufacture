function [ reducedBasisFuns ] = reducedBasis( X, i, iMode derivative, problem, reductionOperator, elementsCoords, refinementDepth )
%% Reduce Order Basis

funs = {@getReducedBasis, @getReducedBasisDerivatives};
[x, elementID] = mapGlobalToLocalAndGetElementsID( X, elementsCoords, refinementDepth);

reducedBasisFuns= funs{derivative+1}(x, i, elementID, reductionOperator );

% % new vector of reduced order basis functions
% reduceBasis =@(x, i) problem.basis_fun(x, i, 0)*uNormalized;

end


function y = getReducedBasis( x, i)

    y =@(x, i) problem.basis_fun(x, i, 0)*uNormalized(;

end
